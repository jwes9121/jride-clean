"use client";
export default function Page(){
  return (
    <div style={{padding:20}}>
      <div style={{fontSize:28, fontWeight:700, marginBottom:12}}>JRIDE LIVETRIPS V2 TEST PAGE</div>
      <div style={{
        height:520, background:"#fff", borderRadius:16, boxShadow:"0 2px 10px rgba(0,0,0,.08)",
        display:"grid", placeItems:"center"
      }}>
        <div>Map container placeholder (this confirms app router + styles are rendering)</div>
      </div>
    </div>
  );
}
export const dynamic = "force-dynamic"; export const revalidate = 0;
export const dynamic = "force-dynamic"; export const revalidate = 0;
