"use client";
export default function DispatchPageComponent() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Dispatch (smoke test)</h1>
      <p className="text-sm text-gray-600">If you can build now, we’ll paste the full UI next.</p>
    </div>
  );
}