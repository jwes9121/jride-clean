# Timeout Data Integrity Policy

JRide must not label a later maintenance timestamp as an exact customer-facing timeout event. Exact event time is displayed only when captured at the actual state transition. Historical records without that event show a derived response deadline and an explicit quality label.
